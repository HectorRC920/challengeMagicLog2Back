
const {
  host,
  username,
  password,
  database,
  port,
  dialect,
  poolConfig,
} = require("../../config/index");
module.exports = {
  host,
  username,
  password,
  database,
  port,
  dialect,
  poolConfig,
};
